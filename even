#include<stdio.h>
int iseven(int num)
{
	return !(num & 1);
}
int main()
{
	int num;
	printf("enter any number");
	scanf("%d",&num);
	if(iseven(num))
	{
		printf("number is even");
	}
	else
	{
		printf("number is odd");
	}
}
